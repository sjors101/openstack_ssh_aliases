# openstack_ssh_aliases
Create ssh aliases based on dynamic infrastructure

## Manual tasks:
 * Add stuff to secret.json
 * Set output_file and ssh_user
 * run script / create cron
 * add location to .bashrc or whatever
